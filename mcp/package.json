{
  "name": "body-iq-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server over the Body IQ knowledge graph — dataset-backed, no database required.",
  "bin": {
    "body-iq-mcp": "./server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.0"
  }
}
